const express = require('express');
const router = express.Router();

// update a existing board
app.post('/:team_id/:id', (req, res) => {
  
});

// get a board JSON
app.get('/:team_id/:id', (req, res) => {
  
});

// delete a board
app.delete('/:team_id/:id', (req, res) => {
});

module.exports = router;
